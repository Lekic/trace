%table.table.table-striped
	%tr
		%th Name
		%th Email
		%th Username
		%th Admin
		%th Date Joined
		%th Edit User
	- @users.each do |user|
		%tr
			%td= user.name
			%td= user.email
			%td= "@" + user.username
			%td= user.admin
			%td= user.created_at.strftime("%m/%d/%Y")
			%td= link_to "Edit", edit_user_path(user)