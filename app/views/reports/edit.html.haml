.row
	.col-xs-10.col-xs-offset-1
		%header.clearfix
			.pull-left
				%h2 Edit Report
			.pull-right
				= link_to "Delete", report_path(@report), method: :delete, data: { confirm: "Are you sure you want to delete this entry?" }, :class => "btn btn-danger"
		%hr
		= render 'report_form'