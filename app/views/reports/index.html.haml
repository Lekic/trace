%h1 Reports

= link_to "Create New Report", new_report_path
-#above line differs from others, don't know why.

%table
	%tr
		%th Name
		%th Reports
		%th Edit
		%th Delete
	- @reports.each do |report|
		%tr
			%td= link_to report.id, report_path(report)
			%td Temp Field
			-#%td- sightings.each do |sighting|
				-#%li= link_to sighting.id
			%td= link_to "Edit", edit_report_path(report)
			%td= link_to "Delete", report_path(report), method: :delete, data: { confirm: "Are you sure you want to delete this entry?" }