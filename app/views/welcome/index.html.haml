.jumbotron
	%h1= "Welcome back @" + current_user.username + "!"
	%p
		So far today,
		= pluralize(@pests_today.length, "pest")
		have been created and
		= pluralize(@sightings_today.length, "sighting")
		have added to the system.
	.btn.btn-primary.btn-lg Report a sighting